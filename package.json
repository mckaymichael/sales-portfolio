{
  "name": "sales-portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run optimize",
    "dev": "npm-run-all --parallel dev:*",
    "dev:server": "live-server",
    "dev:assets": "node scripts/optimize.mjs --watch",
    "optimize": "node scripts/optimize.mjs"
  },
  "devDependencies": {
    "chokidar": "^5.0.0",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sharp": "^0.34.5"
  }
}
